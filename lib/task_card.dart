import 'package:flutter/material.dart';
import 'package:flutter_todo_app/task_tile.dart';

class TaskTile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(child: TaskTileWidget());
  }
}
